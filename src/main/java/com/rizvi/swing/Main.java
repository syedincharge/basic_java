package com.rizvi.swing;

import javax.swing.*;
import java.awt.*;

public class Main {


    public static void main(String[] args) {

        //  JFrame  = a GUI window to add components to
/*
        JFrame frame  = new JFrame(); // create the frame
        frame.setTitle("JFrame title goes here");// sets the title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of application
        frame.setResizable(false);// prevent frame from being resized
        frame.setSize(620,620); // sets the x-dimension, y-dimension of Frame
        frame.setVisible(true);  // make frame visible

        ImageIcon image = new ImageIcon("test.jpg"); // create an ImageIcon
        frame.setIconImage(image.getImage()); // change icon of frame
        frame.getContentPane().setBackground(new Color(126,50,255)); // change background of frame*/

     new MyFrame(); // create the instance of JFrame


    }
}
